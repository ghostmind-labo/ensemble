{
  "mcpServers": {
    "ensemble": {
      "command": "sh",
      "args": [
        "-c",
        "if command -v ensemble >/dev/null 2>&1; then exec ensemble mcp serve; else exec npx -y @ghostmind-dev/ensemble@latest mcp serve; fi"
      ]
    }
  }
}
